# Segredos e configuração local — nunca versionar
.env
*.env
!.env.example

# Backups gerados por scripts/backup.sh
backups/

# Logs locais
*.log

# Dados persistentes, caso alguém troque volumes nomeados por bind mounts locais
data/
prometheus-data/
grafana-data/
loki-data/

# Ajustes locais de compose (ex.: compatibilidade Docker Desktop) — específicos
# de cada máquina de desenvolvimento, nunca da VPS de produção.
docker-compose.override.yml

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
